<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-success mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1">Admin-Dashboard</span>
            <a href="/" class="btn btn-outline-light btn-sm">New Simulation</a>
        </div>
    </nav>

    <div class="container">
        <div class="card shadow">
            <div class="card-body">
                <h2 class="mb-4">Simulation History</h2>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Email</th>
                                <th>Purchase</th>
                                <th>Rent</th>
                                <th>Annual Fee</th>
                                <th>Y1 Comm</th>
                                <th>Y2 Comm</th>
                                <th>Y3 Comm</th>
                                <th>Monthly Avg</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% simulations.forEach(sim => { %>
                                <tr>
                                    <td><%= sim.email %></td>
                                    <td>$<%= sim.purchase %></td>
                                    <td>$<%= sim.monthlyRent %></td>
                                    <td>$<%= sim.annualFee %></td>
                                    <td class="text-success fw-bold"><%= sim.resY1 %>%</td>
                                    <td class="text-success fw-bold"><%= sim.resY2 %>%</td>
                                    <td class="text-success fw-bold"><%= sim.resY3 %>%</td>
                                    <td class="text-success fw-bold">$<%= sim.resMonthly %></td>
                                    <td><%= new Date(sim.createdAt).toLocaleDateString() %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>
</html>